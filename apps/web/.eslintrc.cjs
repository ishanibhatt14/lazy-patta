module.exports = {
  extends: ['@lazy-patta/eslint-config'],
  parserOptions: {
    project: './tsconfig.json',
    tsconfigRootDir: __dirname,
    ecmaFeatures: { jsx: true },
  },
  env: { browser: true },
  // next-env.d.ts is generated by Next and references ./.next/types (absent
  // until a build), which trips import/no-unresolved on a clean checkout.
  // public/sw.js is a hand-written service worker served as a static asset; it is
  // not part of the TypeScript project and must not be type-linted.
  ignorePatterns: [
    '.next/**',
    'next-env.d.ts',
    'next.config.mjs',
    'postcss.config.mjs',
    'public/sw.js',
  ],
};
